網址　https://codebeautify.org/pug-to-html-converter

doctype
html
    head
        title express pug
    body
        h1 hello world
        ul
            -var author='esther ying'
            -var fruits=["apple","kiwi","orange"]
            each item,index in fruits
                li #{index} : #{item}
        section.sky
            form(action="/" method="post")
                input(type="text" name="username" placeholder="enter your name here")
            h1 #{author}
            
